#include <unistd.h>
#include "main.h"

/**
 *_putchar - a function that prints character c
 *@c: character to print
 *
 * Return: 1 on success or -1 on error
 */

int main(void)
{
	return (write( 1, &c, 1));
}
