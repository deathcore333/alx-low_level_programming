#include "main.h"

/**
 *_abs - a function that computes an absolute integer
 *@n: the number under tes+t
 * Return: Always 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
