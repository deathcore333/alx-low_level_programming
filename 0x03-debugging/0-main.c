#include "main.h"

/**
 * main - a function that tests and prints whether an integer is positive or negative
 *
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
